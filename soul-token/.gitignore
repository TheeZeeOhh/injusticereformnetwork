# Compiler files
cache/
out/

# Commonly ignored directories
.logs/
.ignore/

# Ignores development broadcast logs
!/broadcast
/broadcast/*/31337/
/broadcast/**/dry-run/

# Docs
docs/

# Dotenv file
.env
.env.*
!.env.example

# Foundry dependencies — restored via `forge install` (see README).
# Not vendored into the Sanctuary repo to avoid bloat.
lib/

# Deploy artifacts (may contain the deployer address; keep local).
broadcast/
