# Trigger directory for the duress panic wipe. Owned by the kiosk user so the
# Sanctuary app can arm the wipe (`touch /run/irn/panic`) without root. On tmpfs
# (/run), so it never persists across a reboot.
d /run/irn 0700 irn irn -
